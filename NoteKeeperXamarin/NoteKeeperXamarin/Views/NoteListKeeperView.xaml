<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="NoteKeeperXamarin.Views.NoteListKeeperView">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Command="{Binding AddNote}"/>
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding NoteItemList}" x:Name="NotesList">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding NoteFileName}" Command="{Binding Path=BindingContext.OpenNote, Source={x:Reference Name=NotesList}}" CommandParameter="{Binding Id}"></TextCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Text="Add Note" HorizontalOptions="FillAndExpand" Command="{Binding AddNote}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>